<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"

    tools:context=".MainActivity">


    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/idRvItems"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        tools:listitem="@layout/kirana_member_item">

    </androidx.recyclerview.widget.RecyclerView>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="100dp"
        android:layout_height="100dp"
        android:layout_alignParentEnd="true"
        android:layout_alignParentBottom="true"
        android:layout_marginStart="20dp"
        android:layout_marginTop="20dp"
        android:layout_marginEnd="20dp"
        android:layout_marginBottom="20dp">

        <Button
            android:id="@+id/mainAddBtn"
            android:layout_width="98dp"
            android:layout_height="90dp"
            android:background="@drawable/button_shape"
            android:contentDescription="TODO"
            android:elevation="9dp"
            app:layout_constraintTop_toTopOf="parent"
            tools:ignore="MissingConstraints" />

        <ImageView
            android:layout_width="85dp"
            android:layout_height="85dp"
            android:src="@drawable/ic_baseline_add_24"
            android:elevation="10dp"
            app:layout_constraintStart_toStartOf="@+id/mainAddBtn"
            app:layout_constraintEnd_toEndOf="@+id/mainAddBtn"
            app:layout_constraintTop_toTopOf="@+id/mainAddBtn"
            app:layout_constraintBottom_toBottomOf="@+id/mainAddBtn"/>

    </androidx.constraintlayout.widget.ConstraintLayout>

</RelativeLayout>